# HZAgentBase 核心依赖
# 从 pyproject.toml 同步，安装时推荐使用 pyproject.toml（pip install .）

deepagents>=0.6.4,<0.7.0
langchain-openai>=0.3.0
langchain-deepseek>=1.0.0
langgraph>=0.4.0
pydantic>=2.0
click>=8.0
rich>=13.0
httpx>=0.24.0
openai>=1.0
python-dotenv>=1.0
